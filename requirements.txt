requests==2.32.5
flask==3.1.3
werkzeug==3.1.4       # pinned explicitly; transitive dep of flask, kept for reproducibility
python-dotenv==1.1.1
urllib3==2.6.0         # pinned explicitly; transitive dep of requests, kept for reproducibility
