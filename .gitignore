__pycache__/
.env
.env.*
!.env.example
.agent/
.idea/
.vscode/
*.pyc
*.pyo
*.pyd
venv/
.venv/
everything-claude-code/

# Untracked local tool config dirs (graph-extraction excludes live in .graphifyignore;
# .claude/ is intentionally NOT ignored here — its skills/agents/commands are tracked)
.opencode/
.agents/

# Vercel CLI artifacts
.vercel/

# Ruflo / claude-flow runtime data (regenerated locally)
.claude-flow/
.swarm/
ruvector.db
.claude/settings.local.json

# Accidental shell-redirect artifacts (stray files from malformed commands)
/$(({}))

# Subagent-driven-development scratch (progress ledger, review packages)
.superpowers/
/$(wc
/.modal-actions
/0.05
/400
/draw(0))
/item.id
/MAX_IMAGE_BYTES
/MAX_STATE_COOKIE_BYTES
/SHIPPERS_REQUIRED
/ship.approved)
/`token`

# Ruflo / claude-flow runtime lock
.claude/scheduled_tasks.lock

# Obsidian workspace data (local only)
.obsidian/

# Graphify knowledge graph output (regenerated locally)
graphify-out/

# Ruff cache
.ruff_cache/

# Pytest cache
.pytest_cache/

# ECC (Elliptic Curve Cryptography) certs and keys
*.ecc
*.pem
*.key
*.crt

# Misc generated / stray files
*.log
.DS_Store
Thumbs.db

# Admin ban list (created at runtime on first ban)
banned_emails.json

# Local git worktrees
.worktrees/
